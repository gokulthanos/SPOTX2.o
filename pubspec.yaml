name: spotx_ride_app
description: "SpotX 4.0 — Smart Transit & Digital Ticketing Platform"
publish_to: 'none'

version: 4.0.0+4

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  http: ^1.2.1
  geolocator: ^12.0.0
  qr_flutter: ^4.1.0
  lucide_icons: ^0.257.0
  intl: ^0.19.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.0.0
  google_maps_flutter: ^2.5.0
  razorpay_flutter: ^1.3.7
  url_launcher: ^6.2.2
  flutter_local_notifications: ^17.2.1
  permission_handler: ^11.3.0
  socket_io_client: ^2.0.3+1
  fl_chart: ^0.68.0
  mobile_scanner: ^5.2.3
  flutter_blue_plus: ^1.31.7
  printing: ^5.12.0
  pdf: ^3.10.8
  cached_network_image: ^3.3.1
  connectivity_plus: ^6.0.1
  shimmer: ^3.0.0
  lottie: ^3.1.0
  json_annotation: ^4.8.1
  # Firebase — Push Notifications
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  mockito: ^5.4.4

flutter:
  uses-material-design: true
  assets:
    - assets/
